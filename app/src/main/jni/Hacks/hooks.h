#ifndef HOOKS
#define HOOKS

namespace Hooks {
    using namespace Menu;
    
 /*
  void (*setShield)(void* _this, float valuez);
  void (*_Shield)(void *instance);
  void Shield(void *instance) {
    if (instance != NULL) {
        if(SWITCH.BoolExample) {
            setShield(instance, 100);
        }
    }
    return _Shield(instance);
}
*/
    

}//Namespace Hooks

#endif HOOKS
